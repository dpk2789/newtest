@model DateTime?
@Html.TextBox("", (Model.HasValue ? Model.Value.ToString("dddd/MMMM/yyyy") : string.Empty), new { @class = "form-control" })

<script type="text/javascript">
    $(document).ready(function () {
        $('.datetimepicker6').datetimepicker({
            //format: 'LT',
            format: 'DD/MM/YYYY',
            //daysOfWeekDisabled: [0, 6]
        });
    });
</script>
